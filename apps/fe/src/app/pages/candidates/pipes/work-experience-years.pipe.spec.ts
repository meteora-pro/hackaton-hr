import { WorkExperienceYearsPipe } from './work-experience-years.pipe';

describe('WorkExperienceYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkExperienceYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
